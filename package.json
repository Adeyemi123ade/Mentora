{
  "name": "mentora",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm-run-all --parallel dev:web dev:server",
    "dev:web": "npm --workspace apps/web run dev",
    "dev:server": "npm --workspace apps/server run dev",
    "build": "npm --workspace packages/shared run build && npm --workspace apps/server run build && npm --workspace apps/web run build",
    "typecheck": "npm --workspace packages/shared run typecheck && npm --workspace apps/server run typecheck && npm --workspace apps/web run typecheck",
    "test": "npm --workspace apps/server run test && npm --workspace apps/web run test",
    "db:generate": "npm --workspace apps/server run prisma:generate",
    "db:migrate": "npm --workspace apps/server run prisma:migrate",
    "db:seed": "npm --workspace apps/server run db:seed",
    "db:studio": "npm --workspace apps/server run prisma:studio"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "supabase": "^2.114.0"
  }
}
